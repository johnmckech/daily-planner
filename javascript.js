var currentDay = moment().format('MMMM Do YYYY, h:mm:ss a');
var displayDay = document.getElementById('currentDay');
displayDay.textContent = currentDay

var pastTime = currentDay--
var futureTime = currentDay++


var createTask = function (event) {
    event.preventDefault();
    var taskNameEl = document.querySelector
};

//make current time variable, then


function calendarColor() {
    if 
} 

calendarColor();
createTask;
    

 
